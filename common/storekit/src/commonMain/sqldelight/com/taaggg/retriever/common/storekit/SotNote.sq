CREATE TABLE sotNote(
  id TEXT UNIQUE NOT NULL,
  title TEXT NOT NULL,
  content TEXT NOT NULL
);

getById:
SELECT *
FROM sotNote
WHERE id = ?;

upsert:
INSERT OR REPLACE INTO sotNote
VALUES ?;

clearById:
DELETE
FROM sotNote
WHERE id = ?;

clearAll:
DELETE FROM sotNote;