CREATE TABLE localPinGraph(
    userId TEXT NOT NULL,
    graphId TEXT NOT NULL,
    FOREIGN KEY (userId) REFERENCES localUser (id),
    FOREIGN KEY (graphId) REFERENCES localGraph (id),
    PRIMARY KEY (userId, graphId)
);

getById:
SELECT *
FROM localPinGraph
WHERE userId = ? AND graphId = ?;

upsert:
INSERT OR REPLACE INTO localPinGraph
VALUES ?;

clearById:
DELETE
FROM localPinGraph
WHERE userId = ? AND graphId = ?;

clearAll:
DELETE FROM localPinGraph;
