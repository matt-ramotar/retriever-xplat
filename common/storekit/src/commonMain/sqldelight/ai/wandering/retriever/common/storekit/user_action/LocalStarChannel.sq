CREATE TABLE localStarChannel(
    userId TEXT NOT NULL,
    channelId TEXT NOT NULL,
    FOREIGN KEY (userId) REFERENCES localUser (id),
    FOREIGN KEY (channelId) REFERENCES localChannel (id),
    PRIMARY KEY (userId, channelId)
);

getById:
SELECT *
FROM localStarChannel
WHERE userId = ? AND channelId = ?;

upsert:
INSERT OR REPLACE INTO localStarChannel
VALUES ?;

clearById:
DELETE
FROM localStarChannel
WHERE userId = ? AND channelId = ?;

clearAll:
DELETE FROM localStarChannel;
