CREATE TABLE localCreateChannel(
    userId TEXT NOT NULL,
    channelId TEXT NOT NULL,
    FOREIGN KEY (userId) REFERENCES localUser (id),
    FOREIGN KEY (channelId) REFERENCES localChannel (id),
    PRIMARY KEY (userId, channelId)
);

getById:
SELECT *
FROM localCreateChannel
WHERE userId = ? AND channelId = ?;

upsert:
INSERT OR REPLACE INTO localCreateChannel
VALUES ?;

clearById:
DELETE
FROM localCreateChannel
WHERE userId = ? AND channelId = ?;

clearAll:
DELETE FROM localCreateChannel;
